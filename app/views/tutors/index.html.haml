%h2 All Tutors for #{@subject.name}

%table#tutors.table.table-striped.col-md-12
  %thead
    %tr
      %th Name
      %th Experience
      %th Rate
      %th Average Review
  %tbody
    - @tutors.each do |tutor|
      %tr
        %td= tutor.name
        %td= (tutor.experience.to_s + " years")
        %td= number_to_currency(tutor.rate)
        %td= link_to (tutor.average_rating.to_s + "/5"),
          subject_tutor_reviews_path(@subject,tutor)
        %td= link_to "Book a session with #{tutor.name}", new_subject_tutor_session_path(@subject,tutor)

= link_to 'Add new tutor', new_subject_tutor_path
= link_to 'Back to subjects', subjects_path